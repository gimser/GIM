export interface Profile { id: string; username: string | null; avatar_url: string | null }
export interface Product { id: string; city_id: string; name: string; price_cents: number; image_url: string | null }
